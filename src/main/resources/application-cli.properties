# ── CLI profile ───────────────────────────────────────────────────────────────
# Applied automatically when the app is launched with --cli.
# Disables embedded Tomcat so no port is bound in command-line mode.
spring.main.web-application-type=none