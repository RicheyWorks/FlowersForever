# Activate with: --spring.profiles.active=demo
# (combine: demo,auth or demo,sqlite)
#
# Seeds sample Kitsap CRM + harvest + today's CONFIRMED orders when those
# areas are empty — so Market Day, morning briefing, and bed production demos
# work without manual data entry.

flowerfarm.demo.seed=true
logging.level.com.flowerfarm.config.DemoDataSeeder=INFO
